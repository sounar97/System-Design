clinet to server(request and response method)
clinet can also be another server using as an api 
RPC(Remote procedure call) it will create a network call in the server side which are explictly written

